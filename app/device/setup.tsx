import { View, Text } from "react-native";

export default function DeviceSetupScreen() {
  return (
    <View className="flex-1 bg-background items-center justify-center">
      <Text className="text-text-primary text-lg">Device Setup</Text>
    </View>
  );
}
